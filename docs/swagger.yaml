definitions:
  modle.Goods:
    properties:
      id:
        description: 商品id
        type: integer
      name:
        description: 商品名称
        type: string
    type: object
info:
  contact: {}
  license: {}
paths:
  /api/home:
    get:
      consumes:
      - application/json
      description: get string by ID
      operationId: get-string-by-int
      parameters:
      - description: Offset
        in: query
        name: offset
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Can not find ID
          schema:
            $ref: '#/definitions/modle.Goods'
      summary: Add a new pet to the store
swagger: "2.0"
